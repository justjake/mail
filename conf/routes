# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

# log in + authentication
GET     /session                                Session.Status
POST    /session/new                            Session.New
POST    /session/destroy                        Session.Destroy

# connect to an IMAP server
GET     /servers                                Servers.Index
POST    /servers/dial                           Servers.Dial
POST    /session/close                          Servers.Close

# query mailboxes on a server
GET     /mail                                   Mailboxes.Index
GET     /mail/:box                              Mailboxes.Show
# get messages from a mailbox 
GET     /mail/:box/:message                     Message.Show

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
